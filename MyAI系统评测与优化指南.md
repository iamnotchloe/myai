# 金融/政务 RAG 问答系统：评测与优化指南

> 本手册基于三类真实材料整理：
>
> 1. `Downloads/实训/7.17` 中的原始代码和 PDF；
> 2. `Desktop/华为实习/myai` 中的当前版本；
> 3. 之前从语雀导出、准备发布到 GitHub Pages 的《RAG》《RAG 2.0》《向量检索》《评估》《Qwen3 Embedding/Reranking》等资料。
>
> `MyAI` 是你在原始实训版本上已经亲自调优过的当前版本，不是未经修改的原始项目。本文严格区分“你已经完成的调优”和“下一轮仍需改进的内容”，项目说明中既要讲清自己的实际工作，也不要把计划方案说成已经上线的结果。

---

## 一、先统一项目口径

### 1. 最稳妥的项目名称

建议项目中称为：

**基于混合检索与重排序的企业金融报告 RAG 问答助手。**

原因：当前真正建立索引的数据是 10 家企业的金融报告，问题主要围绕营业收入、净利润、资产负债率、公司治理和社会责任等内容。

原始前端虽然写着“智能政务问答助手”，目录中也有一份 `政务服务数据.pdf`，但是原始 `build_index.py` 实际只加载了 `18_蓝天旅游有限公司_report.pdf`。因此，如果技术评审追问数据，不能同时声称当前系统完整覆盖金融和政务两个领域。

可以这样解释演进：

> 项目最初是政务问答的前后端原型，后来我把核心 RAG 链路迁移到企业金融报告场景，用多份企业 PDF 验证文档解析、混合检索、重排序、拒答和引用追溯能力。政务 PDF 可以作为第二个知识域继续扩展，但当前完成评测的主域是金融报告。

### 2. 三个版本的演进

#### V0：接口原型

- FastAPI `/query` 接口；
- Gradio/Streamlit 页面；
- 后端通过 `sleep` 模拟模型初始化和回答；
- 目的只是验证前后端通信、会话状态和响应时间展示；
- 还不是真正的 RAG。

#### V1：原始 RAG

- 只索引蓝天旅游 1 份 PDF；
- `RecursiveCharacterTextSplitter`：`chunk_size=200`、`overlap=50`；
- 共 26 个切片，平均约 170 字；
- BGE-small 中文 Embedding；
- FAISS 向量 Top10；
- SiliconFlow 上的 BGE Reranker 取 Top3；
- Qwen/DeepSeek-Qwen 模型生成回答；
- Streamlit 展示答案和切片来源。

#### V2：你已经调优的当前 MyAI 版本

- 10 份企业金融报告；
- 元素级 token 自适应分块：累计上限 448 tokens、完整元素重叠不超过 96 tokens；
- 120 个去重切片，512 维向量；
- FAISS Dense Retrieval Top10；
- BM25 Top20；
- 内容去重、公司元数据过滤；
- BGE Reranker Top3；
- 相关性拒答、数字/实体校验；
- FAQ、用户反馈、few-shot 数据闭环雏形；
- 引用展示使用完整 PDF 页面，并显示文件名和页码。

因此，项目说明中不能把 V2 说成“拿到项目后直接运行”。更准确的说法是：

> 我在原始单文档 RAG 的基础上，扩展了多份企业金融报告，重新调整切片参数，修复重复索引和跨公司元数据错配，加入 Dense + BM25 混合召回、公司过滤、Reranker、拒答、校验与完整页引用。当前版本已经比原始版完整，但仍需要通过固定测试集继续验证和优化。

#### V3：下一轮改进版本

- 建立冻结的 Golden Test Set 和自动化评测流水线；
- 中文 BM25 分词和 RRF 排名融合；
- 保存、分析并校准 Dense、BM25、Reranker 分数；
- 对 Chunk、TopK、阈值和 Embedding 方案做消融实验；
- 完善 FAQ/few-shot/反馈闭环及异步并发能力。

---

## 二、系统架构怎么讲

```text
离线构建：
PDF
 → PyPDFLoader 文本解析
 → 文档清洗与公司元数据标注
 → 元素解析与 token 自适应分块
 → BGE-small-zh-v1.5 生成 512 维向量
 → FAISS IndexFlatL2
 → 同时保存 BM25 文档和页面元数据

在线问答：
用户问题
 → 公司实体/元数据识别
 → FAISS 语义召回 Top10
 → BM25 关键词召回 Top20
 → 合并去重与元数据过滤
 → BGE-reranker-v2-m3 重排
 → Top3 上下文
 → 相关性判断/拒答
 → DeepSeek-R1-Qwen3-8B 生成答案
 → 数字与实体校验
 → 返回答案、公司、PDF 文件、完整页码来源
```

一句话概括：

> 这是典型的 Retrieve-Rerank-Generate 两阶段检索架构。第一阶段追求高召回，第二阶段追求高精度，生成阶段只允许使用重排后的上下文，并把原始 PDF 页作为证据返回。

---

## 三、每一部分为什么这样做

### 1. PDF 解析：为什么使用 PyPDFLoader

当前 PDF 是可复制文本型报告，不是纯扫描件。`PyPDFLoader` 能按页提取文本并保留：

- `source`；
- `page`；
- `total_pages`；
- `start_index`。

这些元数据可以支持引用追溯和父子切片。

局限：

- 表格可能被打乱；
- 页眉页脚可能成为噪声；
- 扫描 PDF 需要 OCR；
- 图表中的数字不能仅靠文本抽取。

改进后的技术说明：

> 对复杂金融报告，应增加版面感知解析。文本段落继续走文本 Embedding，表格转换为 Markdown/JSON，图表通过 OCR 或视觉模型提取，并保留页码、表名、行列标题等结构元数据。

### 2. 切片：为什么从固定 200/50 改为元素级自适应分块

原始参数：

- `chunk_size=200`；
- `chunk_overlap=50`；
- 重叠比例 25%。

问题：金融指标通常和解释信息一起出现，200 字容易把“指标、数值、单位、时间、公司”切开，引用也会从半句话开始。

当前参数：

- 使用 BGE-small 对应 tokenizer 计算真实 token 数；
- 逐个累计标题、正文句子和表格样式元素；
- `max_tokens=448`；
- 仅复制完整尾部元素，重叠预算 `96 tokens`。

优点：

- 块的字符长度会随 token 密度和元素边界动态变化；
- 一般能容纳一组完整财务指标，并避免在句子或表格中间硬切；
- 切片总数从重复的 1120 条清理并重建为 120 条；
- Top3 上下文总长度仍在模型窗口内。

代价：

- 切片太长会降低语义密度；
- 一个切片可能混入多个主题；
- 精确命中某个数字时可能带来额外噪声。

正确的调参方法不是凭感觉，而是做网格实验：

| 实验 | chunk_size | overlap | 需要比较的指标 |
|---|---:|---:|---|
| A | 200 | 50 | Recall@10、MRR、答案完整性 |
| B | 300 | 50 | 同上 |
| C | 500 | 80 | 同上（旧固定长度基线） |
| D | 800 | 120 | 同上 |
| E | 元素级 token 自适应切片 | 448/96 tokens | 同上 + 延迟 |

最终选择应该基于开发集上的 Recall@K、MRR、上下文精度和答案正确率，而不是只看某一道题。

推荐的下一步是父子切片：

- 小切片 200-300 字用于检索；
- 命中后返回所属的完整段落或页面给生成模型和用户；
- 这就是 Small-to-Big / Parent-Child Retrieval。

当前系统已经采用了其中一半思想：模型使用小切片，用户看到完整 PDF 页。

### 3. Embedding：为什么使用 BGE-small-zh-v1.5

项目是中文短问题检索中文长段落。BGE-small 的优势：

- 中文检索能力好；
- 体积小，在 Mac MPS/CPU 上可运行；
- 512 维向量，120 个切片的存储和检索成本很低；
- 适合课程项目和单机原型。

本地模型配置：

- hidden size：512；
- 最大序列长度：512；
- 4 层 Transformer；
- 8 个注意力头。

当前代码的不足：

- 使用通用 `HuggingFaceEmbeddings`；
- 没有显式设置 `normalize_embeddings=True`；
- 没有显式添加中文检索指令。

BGE 官方模型说明建议短 Query 检索长 Passage 时比较以下两种方案：

```text
原始 Query
vs.
为这个句子生成表示以用于检索相关文章：{Query}
```

文档端不加指令。是否使用指令应由业务开发集决定。

下一轮实验：

| 变量 | 候选 |
|---|---|
| Query instruction | 无 / 中文检索指令 |
| 向量归一化 | False / True |
| 模型 | BGE-small / BGE-base / BGE-M3 / Qwen3-Embedding-0.6B |

比较 Recall@5、Recall@10、MRR、编码延迟、内存和索引大小。

### 4. FAISS：为什么使用 IndexFlatL2

当前真实索引：

- `IndexFlatL2`；
- 120 个向量；
- 512 维；
- 精确最近邻检索。

为什么合理：

- 数据量很小，暴力精确搜索足够快；
- Flat 索引没有近似搜索误差；
- 方便先判断问题来自 Embedding 还是数据，而不是 ANN 参数。

技术评审中如果被问到“为什么不用 Milvus/HNSW”：

> 当前只有 120 个向量，分布式向量数据库属于过度设计。小规模使用 FAISS Flat 可以获得精确检索和最低运维成本。当规模达到十万到百万向量、需要动态更新、权限过滤和多实例服务时，再切换 HNSW/IVF 或 Milvus/Qdrant。

扩展后的调参：

- HNSW：`M`、`efConstruction`、`efSearch`；
- IVF：`nlist`、`nprobe`；
- PQ：子空间数量、每段比特数。

这些参数需要在“Recall@K-延迟-内存”三者之间做 Pareto 权衡。

### 5. BM25：为什么加入关键词召回

Dense Retrieval 擅长同义表达和语义相似，但容易混淆：

- 公司名称；
- 年份；
- 金额；
- 指标名；
- 专有名词。

BM25 对这些精确关键词更敏感，因此和 Dense Retrieval 互补。

当前参数：BM25 Top20。

当前重大不足：LangChain 默认 BM25 预处理通常依赖空格切词，而中文没有天然空格。应改为：

```python
def chinese_tokenize(text):
    return jieba.lcut(text)
```

或者使用字符 bigram/ES 中文分词器。否则“使用了 BM25”不等于“BM25 对中文真正有效”。

如果直接使用 `rank_bm25`，还可以调：

- `k1`：控制词频饱和度，通常从 1.2-2.0 搜索；
- `b`：控制文档长度归一化，通常从 0.5-0.8 搜索。

### 6. 混合检索：当前拼接有什么问题

当前实现是：

```text
FAISS Top10 + BM25 Top20 → 内容去重 → Reranker
```

它能提高候选覆盖面，但还不是真正有权重的融合。两个检索器的原始分数不在同一个尺度，不能直接相加。

建议使用 Reciprocal Rank Fusion（RRF）：

```text
RRF(d) = Σ 1 / (c + rank_i(d))
```

`c` 常以 60 作为起点，但仍需调参。RRF 只依赖名次，不依赖不同检索器的分数尺度，适合融合 BM25 和 Dense Retrieval。

需要做的消融实验：

1. Dense only；
2. BM25 only；
3. Dense + BM25 简单合并；
4. Dense + BM25 + RRF；
5. RRF + Reranker。

只有消融结果才能证明每个模块带来了多少提升。

### 7. 公司元数据过滤：为什么有效，风险是什么

当前问题如果明确出现公司全称，会优先锁定该公司文档。它解决了不同公司报告中大量相同财务术语造成的串公司问题。

优点：

- 高精度；
- 低成本；
- 可解释；
- 对金融报告这种天然有租户/实体边界的数据非常合适。

风险：

- 用户可能使用简称、别名、错别字；
- “比较 A 公司和 B 公司”是多实体查询；
- 只靠字符串包含会漏召回。

改进：公司别名表 + NER + 模糊匹配，并支持多公司集合过滤。

### 8. Reranker：为什么使用 bge-reranker-v2-m3

Embedding 是双编码器：Query 和文档分别编码，速度快，适合粗召回；Reranker 是 Cross-Encoder：联合读取 Query-Document，能看到 token 级交互，速度慢但更准确。

所以两阶段架构是：

- 粗召回负责“不漏掉”；
- 精排负责“把真正答案排到前面”。

当前配置：

- 候选：最多约 30 条，或公司过滤后的全部公司切片；
- 模型：`BAAI/bge-reranker-v2-m3`；
- 输出：Top3；
- API 超时：20 秒。

为什么 Top3：

- 控制上下文长度；
- 减少无关信息；
- 降低 Lost-in-the-Middle；
- 当前问题多为单事实问题，1-3 个片段通常足够。

Top3 不是固定真理，应实验 Top1/3/5。

当前代码把 `relevance_score` 排序后丢弃，这是重要问题。应保留：

- 原始 Reranker 分数；
- 排名；
- 使用的阈值；
- 每阶段耗时。

注意：不同 Reranker 的分数未必是概率，阈值不能凭经验直接设为 0.5，必须在标注开发集上画 PR/ROC 或遍历阈值。

### 9. 相关性拒答

当前使用 Top3 文档与 Query 的平均余弦相似度，阈值 `0.5`。

问题：

- 平均值会被第 2、3 条低分文档拉低；
- Embedding 分数未校准；
- 已经调用 Reranker，却没有使用更强的 Reranker 分数；
- 0.5 没有来自验证集。

更合理的方法：

1. 使用 Top1 Reranker 分数做拒答；
2. 或使用 Top1 + Top3 分数差；
3. 在“可回答/不可回答”标注集上选择阈值；
4. 同时评估拒答 Precision、Recall 和 F1。

### 10. 生成模型与参数

当前：

- 模型：`deepseek-ai/DeepSeek-R1-0528-Qwen3-8B`；
- `temperature=0.1`；
- `max_tokens=1024`；
- 最多重试 3 次；
- 单次超时 120 秒。

为什么低温度：金融事实问答强调稳定和忠实，不追求创意；0-0.2 比较合适。

调参建议：

| 参数 | 候选 | 观察指标 |
|---|---|---|
| temperature | 0 / 0.1 / 0.2 | 正确率、忠实度、输出稳定性 |
| max_tokens | 256 / 512 / 1024 | 完整性、延迟、成本 |
| context TopN | 1 / 3 / 5 | 正确率、噪声、延迟 |
| Prompt | 基线 / 强约束 / 带引用 | 忠实度、拒答率 |

事实型问题通常 256-512 tokens 已足够。1024 更适合综合分析题。

### 11. 答案校验

当前规则会检查答案中的数字和英文实体是否出现在上下文。

优点：便宜、可解释，能拦截一部分凭空数字。

局限：

- “1 亿元”和“10000 万元”语义相同但字符串不同；
- 数字出现不代表结论关系正确；
- 中文实体没有被完整识别；
- 无法验证否定、因果、比较关系。

改进：

- 金额/百分比单位归一化；
- 结构化抽取 `(公司, 年份, 指标, 数值, 单位)`；
- NLI/LLM Judge 判断每个答案 Claim 是否被上下文支持；
- 关键财务指标优先从结构化表中读取。

### 12. FAQ、反馈和 few-shot：真实现状

当前已经有：

- FAQ 相似度阈值 `0.88`；
- 点赞/点踩反馈；
- 从 useful 反馈生成 few-shot 的脚本。

但目前不能在项目说明中说“反馈闭环已经完善”，因为还有这些代码问题：

1. FAQ 会重复写入同一问题；
2. 失败答案也可能进入 FAQ；
3. FAQ 目前有重复测试数据；
4. `few_shot_text` 虽然被构建，但没有真正拼进最终 Prompt；
5. few-shot 脚本丢失了 timestamp，所谓“取最新 5 条”实际无效；
6. 当前反馈库还是空的，没有线上统计结论。

准确的项目说明：

> 我实现了用户反馈采集和 few-shot 数据闭环的原型，但还没有形成可靠的线上训练闭环。下一步先做审核、去重和防污染，只有人工确认的 positive feedback 才能进入 Prompt 或训练集。

---

## 四、到底怎么知道“召回了什么”

必须区分三个集合：

```text
FAISS 原始 Top10
BM25 原始 Top20
Reranker 最终 Top3
```

网页原先只展示最终 Top3，不能据此推断原始召回。

正确检测方式是在每个阶段记录：

```python
def log_docs(stage, docs):
    for rank, doc in enumerate(docs, 1):
        print({
            "stage": stage,
            "rank": rank,
            "company": doc.metadata.get("company"),
            "source_file": doc.metadata.get("source_file"),
            "page": doc.metadata.get("page"),
            "start_index": doc.metadata.get("start_index"),
            "snippet": doc.page_content[:150],
        })

log_docs("faiss", faiss_results)
log_docs("bm25", bm25_results)
log_docs("rerank", reranked_docs)
```

更完整的 Trace 应包含：

- Query ID；
- 原始问题和改写问题；
- 每个检索器的 doc_id、rank、score；
- RRF 分数；
- Reranker 分数；
- 最终进入 LLM 的 doc_id；
- LLM 回答；
- 延迟、Token、错误码。

这样 Bad Case 才能归因：

| 现象 | 根因 |
|---|---|
| 正确文档没进原始 TopK | 数据/切片/Embedding/BM25 问题 |
| 原始召回有，重排后丢失 | Reranker 或融合问题 |
| Top3 正确但回答错误 | Prompt/LLM/上下文利用问题 |
| 回答正确但引用错误 | 引用映射或父子切片问题 |
| 知识库没有答案却强答 | 拒答阈值/幻觉问题 |

---

## 五、召回率、准确率到底怎么算

### 1. 先建立 Golden Test Set

没有人工标注的标准测试集，就不能严肃地说“召回率 90%”或“准确率 95%”。

每条样本至少包含：

```json
{
  "query_id": "finance_001",
  "question": "滨江消费品有限公司2021年的营业收入是多少？",
  "answerable": true,
  "gold_answer": "8000万元",
  "gold_doc_ids": ["11_report_page_1_chunk_0"],
  "gold_pages": [["11_滨江消费品有限公司_report.pdf", 1]],
  "gold_key_points": ["公司=滨江消费品有限公司", "年份=2021", "营业收入=8000万元"],
  "category": "单文档事实",
  "difficulty": "easy"
}
```

测试集应覆盖：

- 单事实问题；
- 同一公司多指标；
- 多公司对比；
- 时间/事件顺序；
- 用户口语化改写；
- 公司简称和错别字；
- 多跳问题；
- 无答案问题；
- 相似但容易混淆的问题；
- 恶意/提示注入问题。

建议先人工标 100-200 条：开发集用于调参，测试集只在版本定稿时使用。

### 2. Hit Rate@K

如果每个问题只有一个标准页面，最直观的是：

```text
Hit@K = TopK 中包含正确页面的问题数 / 总问题数
```

100 个问题中，92 个问题在 Top10 找到正确页面：Hit@10 = 92%。

项目说明中很多人把它口头称为 Recall@K，但严格来说，多相关文档场景应使用下面的 Recall@K。

### 3. Recall@K

```text
Recall@K(q) = TopK 中相关文档数 / 该问题全部相关文档数
```

例如一个问题有 4 个相关页面，Top5 找到 3 个：Recall@5 = 3/4 = 75%。

宏平均：先算每个问题再平均，保证每个 Query 权重相同。

微平均：把所有 Query 的相关文档数汇总后计算，更受大相关集 Query 影响。

### 4. Precision@K

```text
Precision@K(q) = TopK 中相关文档数 / K
```

Top3 有 2 条相关：Precision@3 = 2/3。

Recall 高但 Precision 低，说明“找得全但噪声多”；Precision 高但 Recall 低，说明“很准但漏信息”。

### 5. MRR

```text
MRR = 平均(1 / 第一个相关文档的排名)
```

排名分别是 1、2、5、未召回：

```text
MRR = (1 + 1/2 + 1/5 + 0) / 4 = 0.425
```

它非常适合单答案问答，因为强调正确文档是否排在最前面。

### 6. NDCG@K

当文档有“完全相关/部分相关/不相关”等分级标注时使用 NDCG。它既考虑相关性等级，也考虑排序位置。

例如：完整包含答案的页面标 3，相关背景页标 1，不相关标 0。NDCG 越高，说明高价值页面越靠前。

### 7. Reranker 怎么测

在同一个候选集合上，比较重排前后：

- MRR@10；
- Recall@3；
- NDCG@10；
- Top1 Accuracy；
- 重排延迟。

如果 Dense Recall@10 很高，但 Rerank Recall@3 下降，说明精排把正例排丢了。

### 8. 答案“准确率”怎么测

“准确率”必须先定义，不应只说一个模糊数字。

#### 数值事实问题

先做单位归一化：

```text
1亿元 = 10000万元 = 100000000元
```

再比较 `(公司, 年份, 指标, 数值, 单位)` 是否一致。

```text
Numeric Accuracy = 数值和单位正确的问题数 / 数值问题总数
```

#### 短答案问题

- Exact Match：归一化后完全相同；
- Token F1：参考答案和生成答案 token 的 Precision/Recall/F1。

#### 长答案问题

- Answer Correctness；
- Answer Relevance；
- Completeness；
- Faithfulness/Groundedness；
- 人工 1-5 分；
- LLM Judge + 人工抽检。

### 9. Faithfulness 怎么测

把答案拆成原子 Claim：

```text
Faithfulness = 被上下文支持的 Claim 数 / 全部 Claim 数
```

例如答案有 4 个事实，其中 3 个能在引用页找到，Faithfulness = 75%。

这比 BLEU/ROUGE 更适合 RAG。BLEU/ROUGE 只看文字重合，不能可靠判断事实是否正确。

### 10. 无答案与拒答怎么测

测试集必须包含 15%-25% 无答案问题。

- 拒答 Precision：所有拒答中，真正无答案的比例；
- 拒答 Recall：所有无答案问题中，成功拒答的比例；
- 拒答 F1；
- 错答率：无答案却编造答案的比例。

### 11. 端到端指标

- End-to-End Correct Rate；
- 引用正确率；
- 引用完整率；
- 可回答问题成功率；
- 无答案正确拒答率；
- p50/p95/p99 延迟；
- 错误率；
- 单请求 Token/成本；
- 用户点赞率；
- 人工转接率。

---

## 六、推荐的完整评测实验

### 1. 数据划分

- 开发集：用于调 chunk、TopK、阈值；
- 测试集：冻结，不参与调参；
- 线上回流集：每周从点踩和失败日志中补充。

### 2. 每次版本必须运行的实验

| 实验 | 改动 | 目的 |
|---|---|---|
| Baseline | 原始 Dense Top10 + Rerank Top3 | 建立基线 |
| Ablation 1 | 仅 BM25 | 看关键词贡献 |
| Ablation 2 | Dense + BM25 | 看混合召回贡献 |
| Ablation 3 | 加 RRF | 看融合贡献 |
| Ablation 4 | 加 Reranker | 看精排贡献 |
| Ablation 5 | 加元数据过滤 | 看实体约束贡献 |
| Ablation 6 | 200/300/500/800 切片 | 找最佳粒度 |
| Model Test | BGE-small/base/M3/Qwen3 | 比精度与延迟 |
| TopK Test | 5/10/20，TopN 1/3/5 | 找上下文最优点 |
| Threshold | 拒答阈值扫描 | 找 Precision/Recall 平衡 |

### 3. 报告表格

| 版本 | Hit@10 | Recall@10 | MRR@10 | Rerank Recall@3 | Answer Accuracy | Faithfulness | p95延迟 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Baseline | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| + BM25 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| + RRF | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| + Metadata | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |

没有跑出数字之前，不要在技术说明里虚构提升百分比。

---

## 七、当前系统已经能证明什么

真实完成的事实：

- 原始 7.17 索引：1 份蓝天旅游 PDF、26 个切片；
- 当前索引：10 份 PDF、120 个唯一切片；
- 曾发现错误索引有 1120 条记录但仅 112 条唯一正文，每条正文被绑定到 10 家公司；
- 重建后：112 条记录、112 条唯一正文、跨公司错配为 0；
- 手工端到端测试通过：滨江营业收入、蓝天净利润、绿源营业收入、滨江负债率；
- 测试中答案数值正确，最终引用均来自目标公司；
- 引用从 500 字切片改成完整 PDF 页面。

但目前仍然不能声称：

- “系统准确率达到 95%”；
- “Recall@10 达到 98%”；
- “Reranker 微调过”；
- “FAQ 数据闭环已经上线”；
- “支持完整金融和政务双域”；
- “支持百万级并发或分布式检索”。

原因是还没有冻结的 Golden Test Set 和自动化全量评测报告。

---

## 八、改进优先级

### P0：先让评测可信

1. 建立 100-200 条 Golden Test Set；
2. 为 FAISS、BM25、RRF、Reranker 保存 rank 和 score；
3. 自动计算 Hit@K、Recall@K、Precision@K、MRR、NDCG；
4. 自动计算数值准确率、拒答 F1、Faithfulness；
5. 建立 Bad Case 分类。

### P0：修复当前工程问题

1. BM25 加中文分词；
2. 用 RRF 融合，不只拼接；
3. 保留 Reranker 分数并用开发集调拒答阈值；
4. FAQ 去重、禁止缓存失败答案；
5. 把 `few_shot_text` 真正加入 Prompt；
6. 修复 few-shot timestamp 排序；
7. 同步 `requests` 改为异步客户端或在线程池运行，避免阻塞 FastAPI 事件循环。

### P1：提升检索和上下文质量

1. BGE Query instruction + normalize_embeddings 实验；
2. 父子切片；
3. 公司简称/别名/NER；
4. Query Rewrite；
5. 表格结构化抽取；
6. 财务数值单位归一化；
7. 多查询、多跳问题拆分。

### P2：规模化与生产化

1. 数据版本、增量索引；
2. 权限过滤和租户隔离；
3. Redis 缓存；
4. 结构化日志、Trace ID、监控告警；
5. 限流、熔断、重试退避；
6. HNSW/Milvus/Qdrant；
7. 灰度发布和 A/B 测试；
8. Prompt Injection 和敏感信息安全测试。

---

## 九、项目概述

### 项目摘要

> 我做了一个面向企业金融报告的 RAG 问答助手。离线用 PyPDFLoader 解析 10 份企业报告，按 500 字、80 字重叠切片，用 BGE-small 生成 512 维向量并存入 FAISS。在线采用 FAISS Top10 和 BM25 Top20 混合召回，再通过 bge-reranker-v2-m3 精排到 Top3，最后由 Qwen3-8B 生成答案并返回 PDF 文件和页码。项目中我重点解决了索引重复和跨公司错配问题，并设计了 Recall@K、MRR、答案准确率、Faithfulness 和拒答 F1 的评测体系。

### 技术架构说明

> 项目从一个 FastAPI + Streamlit 的政务原型开始，原始版本只索引一份蓝天旅游报告，使用 200/50 切片、BGE Dense Retrieval Top10、Reranker Top3 和大模型生成。扩展到 10 份金融报告后，我发现旧索引出现严重的数据构建错误：1120 条记录只有 112 条唯一正文，而且同一正文被绑定到 10 家公司。这导致答案看似正确但引用串公司。
>
> 我首先修复构建脚本，每个 PDF 只处理一次，清洗公司名并按公司和标准化正文去重；重建后 112 条全部唯一。在线检索增加 BM25，与 Dense Top10 合并，同时在问题明确提到公司时做元数据过滤，再用 Cross-Encoder Reranker 取 Top3。模型仍用精准切片回答，但引用给用户展示完整 PDF 页，保证可追溯。
>
> 对评测我不会只看最终答案，而是分层测：检索看 Hit@K、Recall@K、Precision@K、MRR；重排看 Recall@3、NDCG；生成看数值准确率、Faithfulness、完整性和拒答 F1；系统层看 p95 延迟、错误率和用户反馈。当前完成了 4 条人工回归，但还没有足够大的 Golden Test Set，因此不会虚构整体准确率。下一步优先做中文 BM25 分词、RRF、保留 Reranker 分数和自动化评测。

### 问题定位与改进复盘

- Situation：多份金融报告术语高度相似，系统出现跨公司引用和“回答正确但证据错误”。
- Task：定位是数据、召回、重排还是生成问题，并提高可解释性。
- Action：分阶段打印检索结果；统计索引唯一正文和公司标签；发现 1120/112 的十倍重复；修复构建和元数据；重建索引；增加公司过滤和完整页引用；对多家公司做回归。
- Result：索引错配从 112 条降到 0；4 条人工回归的答案和公司引用均正确。由于尚未完成大规模 Golden Set，只报告这些可验证结果，不夸大整体准确率。

---

## 十、常见技术问题与设计说明

### 架构与数据

**Q1：为什么要用 RAG，不直接让大模型回答？**

A：企业报告属于私有、可更新知识。RAG 能降低幻觉、提供引用，并避免每次知识更新都微调模型。

**Q2：RAG 和微调的区别？**

A：RAG 主要解决知识注入和时效性，微调主要改变模型行为、风格和任务能力。金融事实优先 RAG，固定输出规范可以用 SFT/DPO 辅助。

**Q3：为什么原始版本表现不好？**

A：只索引一份 PDF、切片偏小、只有 Dense Retrieval，没有系统评测；扩展多文档时还出现重复构建和标签错配。

**Q4：如何处理 PDF 表格？**

A：普通段落文本切片，表格单独结构化为 Markdown/JSON，保留表名、行列标题和页码；扫描件先 OCR，图表可用 VLM。

### 切片

**Q5：Chunk 越大越好吗？**

A：不是。大 Chunk 上下文完整但语义密度低，小 Chunk 检索精准但容易断句。应通过 Recall@K、MRR、答案完整性和延迟联合选型。

**Q6：Overlap 为什么是 80？**

A：500 字的约 16%，用于减少边界丢失。它是工程起点，不是理论最优，需要在开发集上比较 50/80/120。

**Q7：如何避免引用不完整？**

A：父子切片。小切片用于检索，命中后返回父段落/完整页；当前展示端已经返回完整 PDF 页。

### Embedding 与向量库

**Q8：Embedding 是什么？**

A：把文本映射为稠密向量，使语义相近文本在向量空间距离更近。当前使用 512 维 BGE-small。

**Q9：为什么用 BGE-small？**

A：中文检索效果和资源成本平衡好，Mac 可运行。规模和精度要求提高后再比较 BGE-M3 或 Qwen3-Embedding。

**Q10：余弦相似度和 L2 的关系？**

A：向量 L2 归一化后，最小化 L2 距离和最大化余弦相似度的排序等价；未归一化时不等价。当前应实验 normalize_embeddings。

**Q11：为什么 FAISS Flat？**

A：只有 120 个向量，Flat 是精确搜索，速度足够且无 ANN 召回损失。

**Q12：数据到百万级怎么办？**

A：HNSW/IVF-PQ 或 Milvus/Qdrant，配合分区、元数据过滤、增量更新，并测试 Recall-Latency-Memory。

### 检索与排序

**Q13：为什么 Dense 和 BM25 都要用？**

A：Dense 解决语义和同义表达，BM25 解决公司、年份、金额等精确词，二者互补。

**Q14：BM25 的核心思想？**

A：综合词频 TF、逆文档频率 IDF 和文档长度归一化；k1 控制词频饱和，b 控制长度惩罚。

**Q15：中文 BM25 的坑？**

A：不能直接按空格切词，应使用 jieba、字符 n-gram 或专业中文分析器。

**Q16：为什么需要 RRF？**

A：BM25 和 Dense 分数尺度不同，RRF 用排名倒数融合，避免直接归一化分数的困难。

**Q17：Embedding 和 Reranker 的区别？**

A：Embedding 双编码器快，适合大规模召回；Reranker 联合编码 Query-Doc，交互更充分但慢，适合几十条候选精排。

**Q18：为什么 Top10 后取 Top3？**

A：Top10 保证候选覆盖，Top3 控制噪声和上下文成本。具体值要通过 Recall@10、Recall@3 和答案正确率调。

**Q19：如何构造 Reranker 微调数据？**

A：`(query, positive, hard negative)`。Hard Negative 从当前 Dense/BM25 TopN 中排除正例后获取；指标用 MRR@10、Recall@3、NDCG@10。

### 评测

**Q20：召回率怎么算？**

A：Recall@K = TopK 找到的相关文档数 / 全部相关文档数。若每题只有一个标准页面，也常报告 Hit@K。

**Q21：Precision@K 和 Recall@K 区别？**

A：Precision 看 TopK 中噪声多少，Recall 看相关信息漏了多少。

**Q22：MRR 适合什么？**

A：单答案检索，强调第一个正确文档排得是否靠前。

**Q23：NDCG 适合什么？**

A：文档有多级相关性、同时关注整体排序质量时。

**Q24：最终答案准确率怎么测？**

A：数值题先归一化单位后做结构化字段准确率；短答案 EM/Token F1；长答案评估 Correctness、Completeness、Faithfulness。

**Q25：为什么不能只用 BLEU/ROUGE？**

A：它们主要衡量文字重合，无法可靠判断事实是否被上下文支持；RAG 更需要 Faithfulness 和人工/LLM Judge。

**Q26：怎么测试拒答？**

A：加入无答案问题，计算拒答 Precision/Recall/F1 和无答案幻觉率。

**Q27：如何避免 LLM Judge 偏差？**

A：固定 rubric、随机交换 A/B 顺序、多 Judge 投票、人工抽检、校准 Judge 与人工一致率。

**Q28：没有 Golden Set 能不能报准确率？**

A：不能。几条 Demo 只能证明链路可用，不能代表整体准确率。

### 生成与幻觉

**Q29：temperature 为什么 0.1？**

A：事实问答追求稳定和忠实，低温度减少随机性；仍需通过多次运行一致性和准确率验证。

**Q30：如何降低幻觉？**

A：提高检索质量、Rerank、相关性拒答、强约束 Prompt、结构化校验、Claim-Level Faithfulness、人工反馈闭环。

**Q31：上下文正确但答案错怎么办？**

A：属于生成问题。检查 Prompt、上下文顺序、Lost-in-the-Middle、数值计算、模型能力和输出校验。

**Q32：如何处理多跳问题？**

A：Query Decomposition，将问题拆成子问题，分别检索，再聚合证据；必要时 Agentic RAG 迭代检索。

### 工程与生产

**Q33：为什么 FastAPI？**

A：类型校验、自动 OpenAPI、异步能力、性能和部署生态好，适合作为模型服务层。

**Q34：当前并发问题是什么？**

A：async 接口内部使用同步 `requests` 和本地计算，会阻塞事件循环；应使用 httpx.AsyncClient、线程池或任务队列。

**Q35：如何监控？**

A：记录阶段耗时、TopK、分数、Token、错误码、拒答率、点赞率；看 p50/p95/p99，建立 Trace ID。

**Q36：如何缓存？**

A：标准化 Query 后缓存检索结果或答案，但必须绑定知识库版本；低置信度、个性化和敏感查询不缓存。

**Q37：知识库更新怎么办？**

A：文档版本号、内容 hash、增量切片、增量向量、旧版本下线，测试集回归后再切流量。

**Q38：如何防 Prompt Injection？**

A：把检索文档当数据而不是指令；系统 Prompt 明确优先级；过滤恶意片段；工具权限隔离；输出和引用审计。

**Q39：如何保证不同公司数据不串？**

A：公司实体识别 + 元数据过滤 + 访问控制；公司名只是业务过滤，生产环境还需要租户权限校验。

**Q40：如果有更多时间，最先做什么？**

A：先做 Golden Set 和自动化分层评测，然后修中文 BM25、RRF 和阈值；没有评测就无法证明后续优化有效。

---

## 十一、项目说明常见问题

1. 把最终 `source_documents` 说成原始召回结果；
2. 把 Hit@K 和严格的 Recall@K 混为一谈；
3. 没有测试集却报“准确率 95%”；
4. 说使用 BM25，却不知道中文分词问题；
5. 说 FAISS 提高召回率，却不知道当前 Flat 是精确检索；
6. 说 Reranker 分数阈值 0.5，却没有校准；
7. 把 FAQ 和 few-shot 原型说成完整数据飞轮；
8. 把金融报告数据说成政务知识库；
9. 把外部 API 调用说成本地部署大模型；
10. 只讲模型，不讲数据质量、评测、延迟和安全。

---

## 十二、最终记忆框架

只要记住下面六句话，就能覆盖大多数追问：

1. **数据决定上限**：先保证解析、切片、元数据和去重正确。
2. **粗召回追求不漏**：Dense 负责语义，BM25 负责精确词。
3. **精排追求靠前**：Cross-Encoder 把正确证据排进 Top3。
4. **生成必须有依据**：低温度、拒答、结构化校验和引用追溯。
5. **评测必须分层**：召回、重排、生成、端到端、线上指标分别测。
6. **没有 Golden Set 不报准确率**：所有优化都用消融实验和固定测试集证明。
