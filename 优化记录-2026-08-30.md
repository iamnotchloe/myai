# MyAI 优化记录

## 本轮目标

根据第一版离线评测，优先修复有明确证据的问题，不改未经校准的拒答阈值，也不盲目替换大模型。

## 已完成

1. BM25从默认空白分词改为中文字符unigram+bigram。
2. Dense与BM25从简单拼接改为RRF排名融合，默认`RRF_K=60`。
3. Dense、BM25、RRF、Reranker均保留rank和score。
4. `/rag_query`增加可选`debug`参数；为`true`时返回四阶段检索详情并绕过FAQ。
5. Reranker API的`relevance_score`不再丢弃。
6. 问题明确提到公司时，先过滤两路排名，再把目标公司其余切片作为低优先候选交给Reranker。
7. few-shot示例真正插入生成Prompt，并补充无证据拒答、公司/单位约束和Prompt Injection防护。
8. FAQ改为按问题更新、自动去重，拒答和错误答案不缓存。
9. few-shot筛选保留timestamp，并按问题选择最新优质反馈。
10. 检索参数加入`.env.example`，方便后续开发集调参。

## 离线结果

第一版27条可回答题、页面级评测：

| 方法 | Hit@1 | Hit@3 | Hit@10 | MRR@10 |
|---|---:|---:|---:|---:|
| 中文字符BM25 | 0.889 | 0.963 | 1.000 | 0.932 |
| BGE Dense | 0.852 | 0.963 | 1.000 | 0.910 |
| 中文BM25 + Dense RRF | 0.852 | 1.000 | 1.000 | 0.920 |

## 端到端回归

- 单公司数值题：滨江2021年营业收入8000万元，答案和第1页引用正确。
- 多公司比较题：滨江与澜赋营业收入均为8000万元，两家公司第1页均被引用。
- 无答案题：上海明天天气，系统正确拒答且不返回来源。
- Reranker分数现在可以查看：单公司正确证据Top1约0.9998；天气问题Top1约0.00015。

这些只是小规模回归，不代表整个系统准确率。

## 如何查看召回过程

请求体增加`debug: true`：

```json
{
  "question": "滨江消费品有限公司2021年的营业收入是多少？",
  "debug": true
}
```

响应中的`retrieval_debug`包含：

- `dense`：FAISS原始排名和L2距离；
- `bm25`：中文BM25排名和分数；
- `fused`：RRF融合排名和分数；
- `reranked`：最终Cross-Encoder排名和相关性分数。

## 暂未改动

- `RELEVANCE_MIN_SCORE=0.5`仍保持当前值，需用可回答/无答案开发集扫描后再调整。
- Embedding归一化和Query instruction尚未接入，需要做索引重建对照实验。
- 同步`requests`尚未改成异步客户端。
- 表格结构化解析、Reranker微调和完整线上监控仍属于后续版本。

---

## 第二轮：基于 V2 开发集的优化

### 参数扫描

在 `dev_set_v2.jsonl` 的67条可回答开发题上扫描 `RRF_K` 与 Dense/BM25 权重，最终选用：

- `RRF_K=5`
- `DENSE_RRF_WEIGHT=3.0`
- `BM25_RRF_WEIGHT=1.0`

页面级 RRF 指标由等权基线的 `MAP@10=0.921、NDCG@10=0.944` 提升到
`MAP@10=0.935、NDCG@10=0.953`。

### 后端改进

1. 支持“滨江消费品、阳光传媒、美好家政、ACME”等常用简称，并映射为标准公司名。
2. Reranker候选从重复Chunk改为完整PDF页面，减少同页重复与答案被切断的问题。
3. 单页事实题只返回最相关一页；跨页题返回两页；跨公司题保证每家公司至少一页。
4. 跨页单公司题使用本地页面级中文BM25，跨公司题使用云端Reranker并保留公司覆盖。
5. 云端重排超过10秒时自动回退本地页面级BM25。
6. 新增静态知识边界：实时行情、报告未覆盖年份、API Key/系统提示词、隐私和要求虚构的请求直接拒答。
7. 生成Prompt增加简洁性要求，避免简单问题扩写成无关报告摘要。
8. 数字校验支持差额、比例和单位换算，不再把“238-150=88亿元”等正确计算误判为幻觉。
9. 最终答案为拒答或API错误时不返回来源页，避免误导用户。
10. 请求支持 `retrieval_only=true`，可以只测召回、重排和引用，不调用生成模型。

### V2 开发集回归

- 离线加权RRF：`Recall@10=1.000`、`MAP@10=0.935`、`NDCG@10=0.953`。
- 重排后可回答题：`Citation Hit Rate=1.000`。
- 重排后标准页找全率：优化前 `0.970`，完整页面重排后达到 `0.993`；最后一处跨页题已改用页面级BM25并单独验证标准两页均命中。
- 无关实时问题：检索前0秒拒答，不返回来源。
- 跨公司计算：正确回答阳光传媒238亿元、医疗先锋150亿元、相差88亿元，并只引用两家公司第1页。
- 未披露字段：呼吸机型号与单价问题正确输出“根据现有信息无法回答该问题”。

冻结测试集仍未运行，避免根据测试结果继续调参。

---

## 第三轮：核心财务事实结构化（模型不变）

### 为什么做

财务数值、跨公司差额和比例题并不适合完全依赖生成模型：模型需要重新抄数和计算，速度慢，还可能出现
单位或算术错误。因此增加一个只覆盖高置信核心指标的确定性路由；没有命中或超出边界时仍走原 RAG。

### 已完成

1. 建立 `structured_financial_data.json`，记录9家公司经报告核对的年份、币种、指标值和PDF来源页。
2. 新增 `structured_finance.py`，支持营业收入、净利润、资产、负债、权益、现金流、负债率、资产负债率和ROE。
3. 支持单公司多指标、两家公司相等/大小/差额比较，以及由净利润和营业收入确定性计算净利率。
4. 支持按问题要求换算万元、亿元、万美元和亿美元，数值统一用 `Decimal` 计算。
5. 年份不一致、解释分析题、缺失指标和人民币/美元直接比较会回退 RAG。
6. `/rag_query` 调试信息增加 `route`，可以明确区分 `structured_finance`、`knowledge_boundary` 和 `rag`。
7. 新增结构化单元测试，同时检查每个数据值可解析、指标完整且声明的PDF来源页确实存在于索引。
8. 评测脚本新增路由分布统计，便于计算不同链路的延迟和准确率。

### 实测结果

- V2开发集80题中：`structured_finance=20`、`knowledge_boundary=7`、`rag=53`。
- 引用命中率：`1.000`；标准来源页召回率：`0.993`；接口错误：`0`。
- retrieval-only 平均延迟：第二轮的 `0.881秒` 降至 `0.544秒`，下降约38.3%。
- 命中结构化路由的财务题本机接口约 `1～6毫秒`，并且不消耗生成模型API调用。
- 冻结测试集仍未运行；以上结果只用于开发集验证，不能当作最终泛化成绩。

### 当前边界

- 结构化库只收录报告中明确出现且已人工核对的核心财务数据，不从模型回答反向写入事实库。
- 原因、趋势、业务事件和长答案仍由 RAG 负责。
- 若增加或替换PDF，需要重新核对结构化数据并运行单元测试；不能只改JSON而不检查来源页。
- Embedding、Reranker、LLM及其模型名称在本轮均保持不变。
